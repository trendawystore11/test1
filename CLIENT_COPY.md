# دليل إطلاق نسخة عميل جديد (Client Copy)

هذا المشروع نظام إدارة بيع يخدم عملاء مختلفين. كل عميل يحصل على **نسخة مستقلة** بنظامه وبياناته الخاصة.

**المبدأ:** تعديلات العميل مقتصرة على 3 ملفات — الباقي كود مشترك لا يُلمس.

---

## الخطوات المطلوبة لكل عميل

### 1) هوية المتجر وقواعد البيزنس
الملف: `src/client/config.js`

| القسم | القيم |
|---|---|
| `clientId` | معرّف فريد قصير (حروف/أرقام) **مختلف لكل عميل** — بادئة كل مفاتيح التخزين المحلي |
| `appName` / `tagline` | اسم المتجر والسطر التعريفي |
| `logo` | اسم ملف الشعار داخل `public/` (ضع الصورة أولاً) |
| `primaryColor` / `theme` | اللون الرئيسي والمظهر الافتراضي |
| `currency` | `symbol` (مثل `ج.م`، `ر.س`، `د.إ`) و `locale` (مثل `ar-EG`) |
| `region.timeZone` | توقيت النظام (مثال `Africa/Cairo`) — يُستخدم في كل التواريخ المالية وفلترة «فواتير اليوم» |
| `region.defaultGovernorate` | المحافظة/المنطقة الافتراضية |
| `region.governorates` | قائمة المحافظات/المناطق والمدن (لعميل خارج مصر استبدلها بالكامل) |
| `customerCategories` / `defaultCustomerCategory` | تصنيفات العملاء |
| `expenseCategories` | أصناف المصروفات |

كل ما سبق كان مبعثراً في `settings.js` و`formatters.js` و`egypt.js` و`customerRules.js` و`expensesStore.js` — وهي الآن كلها تقرأ من `config.js` فقط.

### 2) عزل البيانات (Firebase) — إلزامي لكل عميل
> عزل البيانات **لا** يتم في config — يتم بمشروع Firebase مستقل.

1. أنشئ مشروعاً جديداً في [Firebase Console](https://console.firebase.google.com) باسم العميل.
2. فعّل **Firebase Authentication** (بريد/كلمة سر) و **Cloud Firestore**.
3. ضع قواعد الأمان: انسخ محتوى `firestore.rules` إلى المشروع الجديد — القواعد **صارمة** منذ V3.45: لا «auth != null»؛ الوصول لأعضاء الفريق المسجَّلين في سجل الأدوار `staff/{uid}` فقط، والكتابة في الحسابات/الإعدادات للمدير فقط. راجع قسم «النشر الآمن» أدناه.
4. في `src/services/db.js` استبدل `FALLBACK_FIREBASE_CONFIG` بقيم المشروع الجديد:
   - `apiKey` • `authDomain` • `projectId` • `storageBucket` • `messagingSenderId` • `appId` • `measurementId`
5. (اختياري) يمكن للمدير تغيير الربط من الواجهة عبر «إعدادات الربط والسحابة» — تُحفظ في `bms_<clientId>_firebase_config` بمتصفح الجهاز.

**النتيجة:** كل عميل يرى بياناته فقط، حتى لو نُشر بنفس الكود.

### 2أ) النشر الآمن لقواعد Firestore (V3.45 — إلزامي)
> خلفية: `request.auth != null` كانت ثغرة حرجة — أي حساب وهمي يُفتح بالـ apiKey العام يقرأ/يكتب كل شيء. الحل: سجل أدوار `staff/{uid}` (مفتاح المستند = Firebase Auth UID) تعتمده القواعد، ولا يُحسب مستخدم كعضو نظام إلا بوجود مستنده فيه.

**ترتيب النشر (لا تعكسه):**
1. **انشر كود التطبيق الجديد أولاً** — الآن إنشاء الحسابات يتم عبر REST (identitytoolkit) حتى تبقى جلسة المدير، والتطبيق يزامن `staff/{uid}` تلقائياً مع كل عملية حسابات + backfill لحقل `uid` عند الدخول.
2. **فعّل المدير الأساسي (bootstrap يدوي مرة واحدة):** من Firebase Console → Authentication → انسخ `User UID` للمدير، ثم في Firestore أنشئ مستنداً:
   - المسار: `staff/<UID>` — الحقول: `email` (بريد المدير)، `role: "admin"`، `userId: "USR-1001"`.
   - بدونها ستُرفض كل قراءة/كتابة من حساب المدير بعد نشر القواعد (رسالة «حسابك غير مُفعَّل في نظام هذا المتجر»).
3. **انشر `firestore.rules` الجديدة** في المشروع.

بعد النشر: أي حساب مسجّل من خارج التطبيق (حتى عبر SDK بالـ apiKey) لا يستطيع قراءة أو كتابة أي شيء — ليس لديه مستند في `staff`.

**ملاحظات:**
- تغيير كلمة سر الموظف لنفسه يعمل عبر استثناء «تحديث الذات» في قواعد `users` (كل الحقول ثابتة ما عدا `password`/`updatedAt`)؛ إضافة `uid` لأول مرة أيضاً.
- أدوار داخل الفريق الأربعة تقرأ/تكتب بيانات العمل كاملة (متجر واحد لكل مشروع) — التقييد بالدور يبقى على مستوى الواجهة.

### 2ب) عزل التخزين المحلي — مكتمل تلقائياً عبر `clientId`
- كل مفاتيح التخزين مسبوقة بـ `bms_<clientId>_` (البيانات، الإعدادات، الجلسة، إعدادات AI، مزامنة جداول البيانات، سجل المحادثة، المدن المخصصة).
- مصدر البادئة الوحيد: `src/client/storage.js` عبر `storageKey()` — عدّل `clientId` في config فقط ولا تلمس الملف.
- النسخ القديمة (قبل البادئة) تُنظَّف تلقائياً أثناء الترقية؛ وأثناء «مسح قاعدة البيانات» لا تُمسّ مفاتيح عميل آخر أبداً (`isOwnKey`).
- **النتيجة:** عميلان على نفس المتصفح لا يتداخلان محلياً نهائياً.

### 3) البناء والنشر
- `vite.config.js` → غيّر `base`:
  - استضافة على نطاق/مجلد جذر: `base: '/'`
  - GitHub Pages بمجلد فرعي: `base: '/اسم-الريبو/'`
- `index.html` → غيّر `<title>` وأيقونة المتصفح `href="/2.jpg"`.
- `public/` → ضع شعار العميل بنفس اسم `CLIENT.logo` (أو عدّل الاسم).

### 4) التحقق قبل التسليم
```
npm install
npm run test        # كل الاختبارات (53 ملفاً / 642 اختباراً)
npm run build:offline   # نسخة ملف واحدة تفتح محلياً file://
npm run build        # نسخة النشر السحابي
```
ثم جرب أدوار الأربعة (مدير/كاشير/أمين مخزن/محاسب) على بيانات فارغة.

---

## محظورات / تنبيهات
- **لا تعدّل** الملفات الشائعة (الدومين، المخازن، الواجهات، الأدوار) — تُحدَّث عند تطوير النظام كله فقط.
- **أدوار النظام** (admin/employee/storekeeper/accountant) في `src/services/permissions.js` ثابتة وصلبة — لا تُمركز في config.
- **نموذج الطلب** (آجل/شحن/عربون/مرتجعات) في `src/domain/orders/*` و`src/services/posService.js` خاص بهذا العميل — إن احتاج عميل نموذجاً مختلفاً يُفصل له.
- **لا تغيّر `clientId` لعميل موجود بعد تشغيله** — سيفقد بياناته المحلية المكتسبة أثناء الاتصال. `clientId` يُحدَّد مرة واحدة عند إنشاء النسخة ولا يُعدَّل لاحقاً.

---

## قائمة مراجعة سريعة قبل التسليم
- [ ] `config.js`: `clientId` (فريد) + الاسم/الشعار/اللون/العملة/المنطقة/التصنيفات
- [ ] `db.js`: إعدادات Firebase لمشروع العميل
- [ ] كود التطبيق الجديد منشور أولاً (REST + مزامنة staff تلقائية)
- [ ] `staff/<UID>` مُنشأ للمدير الأساسي (bootstrap) في الكونسول
- [ ] `firestore.rules` الصارمة منشورة في المشروع الجديد (بعد الخطوتين أعلاه)
- [ ] `vite.config.js`: base الصحيح
- [ ] `index.html`: العنوان والأيقونة
- [ ] `public/`: شعار العميل
- [ ] `npm run test` أخضر كاملاً
- [ ] `npm run build` / `build:offline` ناجحان
